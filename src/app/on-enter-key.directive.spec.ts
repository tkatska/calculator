import { OnEnterKeyDirective } from './on-enter-key.directive';

describe('OnEnterKeyDirective', () => {
  it('should create an instance', () => {
    const directive = new OnEnterKeyDirective();
    expect(directive).toBeTruthy();
  });
});
